<%# <div class="layout-background"> %>

  <div class="d-flex align-items-end">

  </div>


<div class="container">

  <div class="text-center mt-3">
    <h1 class="h3 pt-3 pb-0">Hi! </h1>
    <h1 class="h5 pb-3"> What’s your dietary restrictions? </h1>
  </div>


  <form novalidate="novalidate" class="simple_form search" action="/" accept-charset="UTF-8" method="get"><input name="utf8" type="hidden" value="&#x2713;" />
   <!-- <div class="search-form-control form-group"> -->
      <!-- <input class="form-control string required" type="text" name="search[query]" id="search_query" placeholder="Discover restricted food nearby" /> -->
    <div class="card-categories">
<!--
      <div class="form-inline">
      <%= form_tag search_path, method: :get do %>

        <%= text_field_tag :query,
        params[:query],
        class: "form-control",
        placeholder: "Find a name"
        %>
        <%= submit_tag "Search", class: "btn btn-secondary" %>

      <% end %>
      </div>
-->
      <div class="btn veg-pic" style="height: 125px; border-radius: 5px; position: relative;">
        <%= link_to search_path(restriction: "vegan"), style:"position: absolute; top: 0; right: 0; bottom: 0; left: 0; width: 100%; height: 100%;" do %>
        <% end %>
        <p> vegan </p>
        <!-- <button name="vegan" type="submit"> Vegan > </button> -->
      </div>

      <div class="btn veg-pic" style="height: 125px; border-radius: 5px; position: relative;">
        <%= link_to search_path(restriction: "celiac") do %>
      <button name="celiac" type="submit" class="btn cel-pic"> Celiac
      </button>
      <% end %>

      <button name="diabetic" type="submit" class="btn dia-pic">
        <%= link_to "Diabetic >", search_path(restriction: "diabetic") %>
      </button>

    </div>

  </form>


<%# </div> %>
<!-- the list of movies -->





<!-- A grid for the cards-meals # ANDRE INPUTS:
# filter by name on controller
# hidden tag in view, to store restriction
# controller action works with both of the filter-->

<!--
 <div class="cards-meals">
    <%# @meals.each do |meal|%>
      <%#= link_to meal_path(meal) do %>
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%#= cl_image_path meal.photo.key %>')">
          <h6> <b><%#="#{meal.user.name}: "%></b> <%#="#{meal.name} at #{meal.offer_price}$"%> </h6>
        </div>
      <%# end %>
    <%# end %>

  </div>
-->
</div>
